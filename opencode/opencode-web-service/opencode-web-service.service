[Unit]
Description=Opencode web service
After=network-online.target

[Service]
Type=simple
ExecStart=%h/.opencode/bin/opencode web --mdns
Restart=always
# Restart=on-failure
# TimeoutStartSec=600

[Install]
# WantedBy=multi-user.target
WantedBy=default.target

